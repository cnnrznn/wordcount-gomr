apiVersion: batch/v1
kind: Job
metadata:
  name: mapper-1
spec:
  template:
    spec:
      restartPolicy: "OnFailure"
      containers:
      - name: mapper-1
        args: ["-input=moby.txt"]
        image: gomr
        ports:
        - name: mr-port
          containerPort: 3000